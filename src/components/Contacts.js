import React, { Component } from 'react';
import { ContactFaces } from './contacts';

class Contacts extends Component {
  render() {
    return (
      <div>
        <ContactFaces />
      </div>
    );
  }
}

export default Contacts;
