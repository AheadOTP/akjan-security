import React, { Component } from 'react';
import { ContactFaces } from './contactComponents';

class Contacts extends Component {
  render() {
    return (
      <div>
        <ContactFaces />
      </div>
    );
  }
}

export default Contacts;
